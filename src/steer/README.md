# steer

Steering-stage helpers shared by the prompt-profile RFM and concept-vector
steering scripts.

- `prompt_profile_rfm_stage_registry.py`: screened-dataset registry and
  validation helpers for the prompt-profile RFM stage.
- `stage_artifacts.py`: stable artifact records, checksums, and vector bundle
  metadata helpers.
