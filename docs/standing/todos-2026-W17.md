# Standing TODOs - Current Rebuild

Last updated: 2026-04-26 04:23 UTC

Sources:
- `../../roadmap.md`
- `../../backlog.md`
- `../weeks/2026-W17/main-four-dataset-rollout-rebuild-2026-04-23.md`
- `../reference/rollout-bundle-v1-schema.md`

Intent:
- Replace the stale March rollout-stat reports and stale March-trained probe reports.
- The replacement object is the current four-dataset contract with explicit thinking `on` and `off` surfaces.
- Mechanism-analysis work is now in scope when it is tied directly to the
  current rebuilt `rollout_bundle.v1` archives.
- Current source layout is split by top-level package: `src/probe/` for probe,
  rollout, data, and training utilities; `src/steer/` for steering-stage
  registry and artifact helpers.

## 1. Roll Out And Collect Stats

Run and finish the current dataset suite under both thinking modes:

- `LiveCodeBench`
- `TACO-hard`
- `MATH level-5`
- `Omni-MATH >= 7`

Requirements:

- Use the current `Qwen/Qwen3-1.7B` contract from `../weeks/2026-W17/main-four-dataset-rollout-rebuild-2026-04-23.md`.
- Keep thinking `on` and thinking `off` as separate mode-tagged runs.
- Save artifacts with the new `rollout_bundle.v1` logic: one `<base>.jsonl.gz` replay bundle plus one `<base>.json` sidecar per dataset/mode.
- Keep the suite manifest next to the generated bundles.
- Treat failed first-row launches as runtime/config bugs, not results.
- Do not include `LiveCodeBench-extra`; it is a subset of retained `LiveCodeBench`.
- Keep the retained `LiveCodeBench` `release_v6` surface whole at `1055`
  prompts; this is an intentional exception to the larger-than-1000 cap.
- Produce fresh rollout-stat summaries from these new bundles.

Current status:

- The previously documented Slurm IDs `2875`, `2877`, `2878`, `2879`,
  `2880`, `2882`, `2883`, and `2884` are stale historical receipts, not the
  active queue.
- Relaunching the canonical four-dataset thinking `on` / `off` suite from the
  current repo contract. As of `2026-04-24 00:57 UTC`, the non-LiveCodeBench
  pairs are being submitted first; LiveCodeBench still needs a readable
  checkout path containing `lcb_runner/`.
- Slurm IDs `2886` through `2891` were failed startup attempts: the wrapper
  tried to activate `/home/zhiwang/miniforge3/etc/profile.d/conda.sh`, but this
  account uses the repo `.venv` instead. Relaunch with conda env variables
  unset so `slurm/rollout/run_collect_model_stats.sbatch` uses `.venv`.
- Fresh `.venv` non-LiveCodeBench launches submitted at `2026-04-24 01:02 UTC`
  under `outputs/model_stats/main_rollout_stats_rebuild_20260424_0102`:
  `2892` TACO-hard thinking on, `2893` MATH level-5 thinking on, `2894`
  Omni-MATH >= 7 thinking on, `2895` TACO-hard thinking off, `2896` MATH
  level-5 thinking off, and `2897` Omni-MATH >= 7 thinking off.
- Superseding the one-GPU `.venv` launches with a multi-GPU launch shape:
  `dp=4`, `tp=1`, and Slurm `--gres=gpu:4` per dataset/mode job.
- One-GPU jobs `2892` through `2897` were cancelled before the multi-GPU
  relaunch. The `dp=4` relaunch command is ready, but submission from the
  current sandbox failed with `sbatch: error: Error creating slurm stream
  socket: Operation not permitted`.
- `2026-04-24 01:15 UTC`: LiveCodeBench should launch first, but no readable
  checkout containing `lcb_runner/` is available under the checked shared,
  scratch, user, or repo paths. The HF dataset cache exists, but it is not
  sufficient for the native LCB prompt/grader path. A shallow clone attempt
  into `.venv/cache/LiveCodeBench` failed because DNS for `github.com` is
  unavailable in the current sandbox.
- `2026-04-24 01:20 UTC`: retrying LiveCodeBench first after enabling network
  and full filesystem access; target checkout path is
  `/data/shared/projects/LiveCodeBench`, followed by an LCB-only `dp=4`
  thinking `on` / `off` launch.
- `2026-04-24 01:23 UTC`: cloned LiveCodeBench to
  `/data/shared/projects/LiveCodeBench` and submitted LCB-only `dp=4`,
  `--gres=gpu:4` jobs under
  `outputs/model_stats/main_rollout_stats_rebuild_20260424_lcb_first_dp4`:
  `2898` thinking on and `2899` thinking off. Job `2898` is running with
  `CUDA_VISIBLE_DEVICES=1,2,3,4`; `2899` is pending for resources.
- Deleted stale local rollout roots and logs from failed/dry-run/canceled
  relaunch attempts, keeping only the active LCB-first output root and logs.
- Updated `AGENTS.md` and `CLAUDE.md` to distinguish startup-stale failed
  launches, which should be deleted after recording the failure, from mid-run
  partial rollouts, which should be preserved for diagnosis or resume unless
  explicitly removed.
- Launching the remaining non-LCB full-stage jobs into the active LCB-first
  output root with `dp=4`, `tp=1`, and Slurm `--gres=gpu:4`; the remaining-job
  launcher manifest is written separately so the LCB-first manifest is not
  overwritten.
- `2026-04-24 01:31 UTC`: submitted the remaining non-LCB full-stage jobs
  under `outputs/model_stats/main_rollout_stats_rebuild_20260424_lcb_first_dp4`
  with manifest `suite_manifest_remaining_non_lcb.json`: `2900` TACO-hard
  thinking on, `2901` MATH level-5 thinking on, `2902` Omni-MATH >= 7 thinking
  on, `2903` TACO-hard thinking off, `2904` MATH level-5 thinking off, and
  `2905` Omni-MATH >= 7 thinking off. These are pending behind active LCB job
  `2898` and pending LCB thinking-off job `2899`.
- Relaunch should point Hugging Face caches at the shared cache:
  `HF_HOME=/data/shared/huggingface`,
  `HF_DATASETS_CACHE=/data/shared/huggingface/datasets`,
  `HUGGINGFACE_HUB_CACHE=/data/shared/huggingface/hub`, and
  `TRANSFORMERS_CACHE=/data/shared/huggingface/transformers`.
- The existing `/data/shared/huggingface` tree has been opened for all users:
  directories are `drwxrwxrwt` and existing files are world-readable/writable,
  so users outside the `zhiwang` group can use the shared cache.
- `2026-04-24 03:30 UTC`: moving the canonical suite sampling and dataset
  contract out of hardcoded Python defaults into a checked-in rollout config
  file. The launcher should load that config by default and keep explicit CLI
  overrides for controlled reruns.
- `2026-04-24 03:35 UTC`: config-source cleanup completed. The default suite
  config is `configs/rollout/main_rollout_stats_suite.json`, manifests record
  the loaded config path, and dry-run launcher checks confirm both default
  config loading and explicit CLI overrides.
- `2026-04-24 06:16 UTC`: review follow-up kept `LiveCodeBench` uncapped by
  design and made the launcher fail fast unless the supplied checkout root
  contains `lcb_runner/`; the sbatch wrapper now has the same preflight check
  for direct wrapper use.
- `2026-04-24 22:15 UTC`: fixed rollout collection so CPU-only post-hoc
  graders do not run inside GPU Slurm allocations. `livecodebench_codegen` and
  `taco_codegen` now default to `DEFER_CPU_FINALIZE=1` in the suite launcher,
  write sidecars with deferred grading status, and are finalized later via
  `scripts/rollout/collect_model_stats.py --finalize-only` with
  `CUDA_VISIBLE_DEVICES=''` outside Slurm.
- `2026-04-24 22:15 UTC`: added DP resume over both final bundles and
  `__rank*.jsonl.gz` partials, including repair of truncated gzip tails left by
  cancellation. Canceled old-path GPU jobs `2899` (LCB off), `2900` (TACO on),
  and `2903` (TACO off), then relaunched current-code deferred/resume jobs
  `2907` (LCB off), `2908` (TACO on), and `2909` (TACO off).
- `2026-04-24 22:15 UTC`: `2898` LCB thinking-on generation completed and was
  canceled before finishing the old inline grader; its complete ungraded bundle
  was preserved as
  `livecodebench__thinking_on.generated_ungraded_2898.jsonl.gz`, and CPU-only
  finalization is running outside Slurm against the canonical bundle.
- `2026-04-25 19:25 UTC`: checked the long-running active Slurm jobs. Running
  jobs `2902` (Omni-MATH >= 7 thinking on) and `2904` (MATH level-5 thinking
  off) are still in GPU generation, not CPU finalization; logs show active
  per-rank prompt progress and live `VLLM::EngineCore` processes. The pending
  LCB/TACO jobs remain on the deferred-finalization code path.
- `2026-04-25 19:25 UTC`: identified a separate static-DP straggler issue:
  completed ranks can release their VLLM processes while the parent `dp=4`
  Slurm job still holds the allocation until the slowest rank exits. This is
  scheduler allocation waste, not post-hoc evaluation.
- `2026-04-25 19:59 UTC`: job `2902` (Omni-MATH >= 7 thinking on) finished
  and wrote its bundle/sidecar. Its long runtime was generation volume:
  average rollout length was about 19.5k tokens versus about 4.1k tokens in
  the still-running `2904` MATH level-5 thinking-off partials.
- `2026-04-25 23:40 UTC`: checked `2909` (TACO-hard thinking off). It is not
  dead; it is still pending with Slurm reason `Resources` and has not started,
  so no `taco_hard__thinking_off` rank files or logs exist yet. The current
  8-GPU node is occupied by running `2905` and `2908`, each requesting 4 GPUs.
- `2026-04-25 23:44 UTC`: reconciled the three Slurm jobs versus four-plus
  unfinished config states. GPU generation is still outstanding for
  `omni_math_ge7/off`, `taco_hard/on`, and `taco_hard/off`; LiveCodeBench
  generation is complete but CPU-only finalization is outside Slurm. Started
  local CPU-only `--finalize-only` sessions for both `livecodebench/on` and
  `livecodebench/off` with `CUDA_VISIBLE_DEVICES` empty.
- `2026-04-26 00:28 UTC`: expanded sidecar prompt-profile summaries so they
  log prompt-level any/majority counts for both loop detector risk
  (`fraction_loop > 0`, `fraction_loop > 0.5`) and fixed overlong risk
  (`fraction_len_0.5 > 0`, `fraction_len_0.5 > 0.5`), plus matching rates and
  prompt means. Existing rollout-level `loop_fraction` and legacy
  `num_prompt_majority_tail_positive` remain for compatibility. Refreshed the
  completed sidecars already present under the active output root; jobs that
  were already running before this patch may need the same sidecar refresh
  after they finish.

Done means:

- All current datasets have clean thinking-on and thinking-off rollout bundles.
- Fresh rollout-stat summaries exist for the current suite.
- The new summaries are the default replacement for March rollout reports.

## 2. Train All Probes On All Dataset Views

Train the probe surface from the new rollout bundles, separately for thinking `on` and thinking `off`.

Requirements:

- Recompute prompt-level labels from the new bundles, not from March artifacts.
- Build prompt-profile datasets for every retained dataset and both thinking modes.
- Train all required probe views on every dataset/mode object.
- Include the locked heads: regression `mean_relative_length` and binary `majority_s_0.5`.
- Add first-class soft probability labels `fraction_loop` and
  `fraction_len_0.5` so rebuilt prompt-profile probes can train on the raw
  rollout fractions instead of only hard majority labels.
- Keep provenance explicit for dataset, thinking mode, target, activation view, probe preset, split, and source bundle.
- Produce fresh probe summaries across all dataset views.

Current status:

- `2026-04-24 05:39 UTC`: completed and smoke-tested `fraction_loop` /
  `fraction_len_0.5` prompt-profile target support. The canonical rollout
  config now carries `loop_n=30`, `loop_k=20`, and dry-run launcher output
  forwards them as `LOOP_N=30`, `LOOP_K=20`.

Done means:

- Probe reports exist for all current datasets under thinking `on`.
- Probe reports exist for all current datasets under thinking `off`.
- The new probe reports are the default replacement for stale March-trained probe reports.

## 3. First Deeper-Attention Mechanism Experiment

Run the first attention-residual PCA analysis on the rebuilt rollout bundles.

Requirements:

- Use `docs/weeks/2026-W17/deeper-attention-mechanism-analysis-2026-04-25.md`
  as the experiment plan.
- For each retained dataset and thinking mode, select up to `5` looped,
  correct, in-budget rollouts and up to `5` looped, wrong, max-length-hit
  rollouts.
- Keep underfilled buckets strict and report them instead of relaxing criteria.
- Analyze final-layer attention writes and post-attention residual states across
  all occurrences of each selected rollout's triggering `30`-gram.
- Use only current finalized `rollout_bundle.v1` archives; do not use March
  prompt-profile archives for this first W17 mechanism experiment.

Current status:

- `2026-04-26 00:45 UTC`: re-scoped the deeper-attention note into a concrete
  first experiment plan centered on strict 5-vs-5 rollout buckets.
- `2026-04-26 03:54 UTC`: implementing the first-experiment mechanism-analysis
  script, without running the full analysis yet.
- `2026-04-26 04:03 UTC`: added the first-experiment script at
  `scripts/mechanism_analysis/analyze_attention_residual_pca_ngrams.py` and
  smoke-checked syntax, CLI help, and in-memory helper behavior. No real bundle
  selection or model replay analysis has been run yet.
- `2026-04-26 04:18 UTC`: splitting the first-experiment implementation into
  smaller modules so no new Python file carries the whole analysis path.
- `2026-04-26 04:23 UTC`: modular split complete. The CLI wrapper remains at
  `scripts/mechanism_analysis/analyze_attention_residual_pca_ngrams.py`; the
  implementation now lives under
  `scripts/mechanism_analysis/attention_residual_pca_ngrams/`, and every new
  Python file is under `1000` lines. Re-ran syntax, CLI-help, and in-memory
  helper checks; still no real bundle selection or model replay analysis run.

Done means:

- Selection ledgers exist for every available dataset/mode bundle.
- Local and pooled PCA plots exist for the selected rollout buckets.
- The mechanism read is explicitly tied to the rebuilt mode-local bundles.
